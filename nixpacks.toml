[phases.setup]\nnixPkgs = ['nodejs', 'chromium']\n\n[phases.install]\ncmds = ['npm ci']\n\n[start]\ncmd = 'node railway-start.js'\n\n[variables]\nPUPPETEER_EXECUTABLE_PATH = '/nix/store/*/bin/chromium'